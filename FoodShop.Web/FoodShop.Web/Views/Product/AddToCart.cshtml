@model ICollection<AddToCartProductViewModel>

@{
    ViewBag.Title = "My Cart";
    decimal totalPrice = 0;
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="row-cols-2" style="display: flex; justify-content: center">
    <div class="card mb-6">
        <div class="card-body">       
            @foreach (var row in @Model)
            {
                <div class="card mb-3" style="display: flex; flex-direction: row;">
                <h4 class="card-title m-md-3 col-md-4">@row.ProductName</h4>
                <p class="card-text m-md-3 col-md-4">Price: @row.ProductPrice</p>
                <p class="card-text m-md-3 col-md-4">Count: @row.Count</p>
                </div>

                totalPrice += row.ProductPrice * row.Count;

                <div class="col-12 px-1" style="display: flex; flex-direction: row; justify-content: end">
                    <a asp-controller="Product" asp-action="AddToCart" asp-route-id="@row.ProductId" class="btn btn-success btn-sm mb-2 w-50 fw-bold rounded btn-dark-text">+1</a>
                </div>
            }
            <div>
                <p class="m-md-3">Total Price: @totalPrice</p>
            </div>
            <div class="col-6 px-1">
                <a asp-controller="Product" asp-action="BuyProducts" asp-route-id="@Model" class="btn btn-success btn-sm mb-2 w-50 fw-bold rounded btn-dark-text">Buy Products</a>
            </div>
        </div>
    </div>
</div>